
# PDF (Puppeteer). Ruta a un Chrome/Chromium ya instalado. Si se omite, Puppeteer
# usa el Chrome que descarga en ~/.cache/puppeteer al instalar (falla si la ruta
# del perfil tiene acentos). En Docker apunta al chromium del sistema.
PUPPETEER_EXECUTABLE_PATH=""
