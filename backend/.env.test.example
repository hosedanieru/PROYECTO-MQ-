# Copiar a `.env.test`. Lo carga test/setup-e2e.ts para las pruebas de
# integración (`npm run test:e2e`). Apunta a una base SEPARADA: las pruebas
# la vacían entre casos. Si no existe, el setup la crea y aplica migraciones.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/mq_test?schema=public"
TZ=America/Bogota
JWT_SECRET="secreto-de-pruebas-de-al-menos-32-caracteres-xxxxxxxx"
JWT_EXPIRES_IN=1h
PUPPETEER_EXECUTABLE_PATH=""
