# Copiar a `.env.firestore.test`. Para `npm run test:firestore`.
# Debe ser un proyecto APARTE del de producción (id con "test" o "prueba"),
# o el emulador: las pruebas vacían colecciones. NUNCA credenciales aquí.
FIREBASE_PROJECT_ID="maquila-inlotrans-test"
FIREBASE_CLIENT_EMAIL=""
FIREBASE_PRIVATE_KEY=""
# FIRESTORE_EMULATOR_HOST=localhost:8080
JWT_SECRET="secreto-de-pruebas-de-al-menos-32-caracteres-xxxxxxxx"
JWT_EXPIRES_IN=1h
TZ=America/Bogota
